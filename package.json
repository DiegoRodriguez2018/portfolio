{
  "name": "gatsby-portfolio",
  "private": true,
  "description": "Diego Rodriguez portfolio.",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "update-repo-and-build": "npm run update-repos && gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1",
    "update-repos": "node src/tools/getRepos && node src/tools/generateData"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.0",
    "gatsby": "^3.0.0",
    "gatsby-plugin-react-helmet": "^3.3.4",
    "gatsby-plugin-sass": "^2.3.4",
    "react": "^16.13.0",
    "react-dom": "^16.12.0",
    "react-helmet": "^6.1.0"
  },
  "devDependencies": {
    "prettier": "2.0.5",
    "sass": "^1.55.0"
  },
  "repository": {
    "type": "git",
    "url": ""
  }
}
